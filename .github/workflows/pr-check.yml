name: PR Check

on:
  pull_request:
    branches:
      - main
    paths:
      - '**.cs'
      - '**.csproj'
      - '.github/workflows/pr-check.yml'
      - 'src/**'
      - 'tests/**'

permissions:
  contents: read

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore --configuration Release
    
    - name: Run unit tests
      run: |
        dotnet test tests/Spacetime.Common.Tests/Spacetime.Common.Tests.csproj --no-build --configuration Release --verbosity normal
        dotnet test tests/Spacetime.Core.Tests/Spacetime.Core.Tests.csproj --no-build --configuration Release --verbosity normal
        dotnet test tests/Spacetime.Plotting.Tests/Spacetime.Plotting.Tests.csproj --no-build --configuration Release --verbosity normal

  integration-tests:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore --configuration Release
    
    - name: Run integration tests
      run: | 
        dotnet test tests/Spacetime.Plotting.IntegrationTests/Spacetime.Plotting.IntegrationTests.csproj --no-build --configuration Release --verbosity normal
        dotnet test tests/Spacetime.Core.IntegrationTests/Spacetime.Core.IntegrationTests.csproj --no-build --configuration Release --verbosity normal
